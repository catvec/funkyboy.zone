groups:
  - name: Cron
    rules:
      - alert: BackupFailed
        expr: backup_success < 1
        labels:
          service: backup
          severity: critical
        annotations:
          summary: Backup cron failed to run
