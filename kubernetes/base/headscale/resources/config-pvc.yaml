apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: headscale-config
  labels:
    project: headscale
    component: server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-ceph-block