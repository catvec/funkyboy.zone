apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-files
  labels:
    project: media-server
    component: files
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi
  storageClassName: cephfs-media-server