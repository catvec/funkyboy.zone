apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: emby-config
  labels:
    project: media-server
    component: emby
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: cephfs-media-server