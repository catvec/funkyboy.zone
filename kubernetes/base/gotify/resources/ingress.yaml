apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gotify
  namespace: gotify
  labels:
    project: gotify
    component: gotify-server
spec:
  rules:
  - host: "gotify.k8s.funkyboy.zone"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gotify
            port:
              number: 80